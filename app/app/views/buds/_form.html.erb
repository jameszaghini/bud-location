<%= form_for(bud) do |f| %>
  <% if bud.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bud.errors.count, "error") %> prohibited this bud from being saved:</h2>

      <ul>
      <% bud.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :identifier %>
    <%= f.text_field :identifier %>
  </div>

  <div class="field">
    <%= f.label :session_id %>
    <%= f.number_field :session_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
